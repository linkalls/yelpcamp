<% layout("layouts/boilerplate") %>
<div class="offset-3 col-6">
  <div class="alert alert-danger" role="alert">
    <h4 class="alert-heading"><%= err.message %></h4>
    <% if (process.env.Node_ENV !== "production") { %>
    <p><%= err.stack %></p>
    < <% } %>
    <!-- これでスタックとレースが出る よくあるエラーのやつ-->
  </div>
</div>
